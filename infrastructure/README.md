# Infrastructure (not yet implemented)

Docker and systemd unit files land here in Phase 11. `infrastructure/scripts`
is reserved for deployment automation, as distinct from top-level `scripts/`,
which holds ad hoc developer/operator scripts like the network reality check.
